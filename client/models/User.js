export default class User {
  constructor(name, surname, login, password, email) {
    this.name = name;
    this.surname = surname;
    this.login = login;
    this.password = password;
    this.email = email;
  }
}
